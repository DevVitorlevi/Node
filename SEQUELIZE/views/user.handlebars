<link rel="stylesheet" href="/arquivos/table.css">
<div class="table">
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Nome</th>
                <th>Profissão</th>
                <th>NewsLetter</th>
            </tr>
        </thead>
        <tbody>
                <tr>
                    <td>{{user.id}}</a></td>
                    <td>{{user.name}}</a></td>
                    <td>{{user.profissao}}</a></td>
                    <td>{{user.newsletter}}</a></td>
                </tr>
        </tbody>
    </table>

    <div class="actions">
        <button><a href="/">Voltar</a></button>
        <button><a href="/user/edit/{{user.id}}">Editar</a></button>

<form action="/user/apagar/{{user.id}}" method="POST">
    <button type="submit">Apagar</button>
</form>
    </div>